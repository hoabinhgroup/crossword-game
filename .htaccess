<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Nếu file hoặc folder không tồn tại → redirect về index.html
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>